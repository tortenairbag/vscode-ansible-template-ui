{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "module": "CommonJS",
    "target": "ES2021",
    "lib": ["ES2021"],
    "outDir": "../../bin",
    "skipLibCheck": true, /* Cannot handle codemirror, requires DOM in lib to work */
  },
  "include": [
    "../types/*.ts",
    "./**/*.ts",
  ]
}